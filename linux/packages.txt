# Core CLI
git
curl
wget
ca-certificates
zip
tar
unzip
gnupg
gzip
xz-utils
rsync

# Shell UX
bat
jq
tree
yazi (snaps)
startship

# Utils
keychain (ssh-agents, commit from cli without asking credentials)

# Networking / debugging
nmap

# Dev essentials
make
gcc
g++
pkg-config
cloc (count lines of code by language)
golang-go + actionlint (check format for gh actions)
yamllint (check format for general YAML)

# Containers
docker
docker-compose-plugin

# Editors / tools (optional)
micro

# Java tooling (optional)
sdkman

